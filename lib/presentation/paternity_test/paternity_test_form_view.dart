import 'package:flutter/material.dart';
class PaternityTestForm extends StatelessWidget {
  const PaternityTestForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
