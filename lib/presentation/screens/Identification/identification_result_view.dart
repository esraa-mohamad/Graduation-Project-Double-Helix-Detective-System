import 'package:flutter/material.dart';

class IdentificationResult extends StatelessWidget {
  const IdentificationResult({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
