import 'package:flutter/material.dart';
class MissingResult extends StatelessWidget {
  const MissingResult({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
