import 'package:flutter/material.dart';
class MissingForm extends StatelessWidget {
  const MissingForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
